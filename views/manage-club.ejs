<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GIIS Clubs. | Manage Club</title>
    <link rel="icon" type="image/png" href="resources/1.png">
    <style>
        @font-face {
            font-family: 'Garet';
            src: url('fonts/GaretR.otf') format('opentype');
            font-weight: 400;
            font-style: normal;
        }

        @font-face {
            font-family: 'Garet';
            src: url('fonts/GaretH.otf') format('opentype');
            font-weight: 700;
            font-style: normal;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Garet', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(180deg, #f5f7fa 0%, #eef2f7 100%);
            color: #1f2937;
            min-height: 100vh;
        }

        .header {
            background: #ffffff;
            box-shadow: 0 2px 16px rgba(0, 0, 0, 0.08);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 14px 24px;
            gap: 16px;
        }

        .logo-section {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo {
            height: 52px;
            width: auto;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 28px;
            list-style: none;
        }

        .nav-links a {
            color: #1f2937;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
        }

        .nav-actions {
            display: flex;
            gap: 10px;
        }

        .cta-button {
            background: linear-gradient(135deg, #1a237e 0%, #3f51b5 100%);
            color: white;
            padding: 10px 20px;
            border-radius: 999px;
            text-decoration: none;
            font-weight: 600;
            font-size: 13px;
            border: none;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(26, 35, 126, 0.25);
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 36px 24px 80px;
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 12px;
            margin-bottom: 24px;
        }

        .page-title {
            font-size: 28px;
            color: #111827;
        }

        .page-subtitle {
            color: #6b7280;
            font-size: 15px;
            margin-top: 4px;
        }

        .panel {
            background: #ffffff;
            border-radius: 18px;
            padding: 26px;
            box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
            border: 1px solid rgba(15, 23, 42, 0.06);
        }

        .panel-header {
            display: flex;
            flex-direction: column;
            gap: 6px;
            margin-bottom: 22px;
        }

        .panel-header h2 {
            font-size: 20px;
            color: #111827;
        }

        .panel-header p {
            color: #6b7280;
            font-size: 14px;
        }

        .section-block {
            border: 1px solid #eef2f7;
            border-radius: 16px;
            padding: 18px;
            margin-bottom: 18px;
            background: #fafbff;
        }

        .section-heading {
            display: flex;
            flex-direction: column;
            gap: 4px;
            margin-bottom: 14px;
        }

        .section-heading h3 {
            font-size: 16px;
            color: #111827;
        }

        .section-heading span {
            font-size: 12px;
            color: #6b7280;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 16px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-group label {
            font-weight: 600;
            color: #111827;
            font-size: 13px;
        }

        .form-group input,
        .form-group textarea {
            padding: 12px 14px;
            border-radius: 12px;
            border: 1px solid #e5e7eb;
            font-size: 14px;
            font-family: inherit;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
            background: #ffffff;
        }

        .form-group input[type="file"] {
            padding: 10px;
            border-style: dashed;
            background: #ffffff;
            cursor: pointer;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #3f51b5;
            box-shadow: 0 0 0 3px rgba(63, 81, 181, 0.1);
        }

        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }

        .helper-text {
            color: #6b7280;
            font-size: 12px;
        }

        .message {
            padding: 12px 16px;
            border-radius: 12px;
            margin-bottom: 16px;
            font-size: 13px;
        }

        .message.success {
            background: #d1fae5;
            color: #065f46;
            border: 1px solid #a7f3d0;
        }

        .message.error {
            background: #fee2e2;
            color: #991b1b;
            border: 1px solid #fecaca;
        }

        .actions {
            display: flex;
            justify-content: flex-end;
            margin-top: 10px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #1a237e 0%, #3f51b5 100%);
            color: #fff;
            padding: 12px 26px;
            border-radius: 12px;
            border: none;
            cursor: pointer;
            font-weight: 600;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 12px 24px rgba(26, 35, 126, 0.25);
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .page-header {
                flex-direction: column;
                align-items: flex-start;
            }
        }
        /* Disable hover animations */
        *, *::before, *::after {
            transition: none !important;
            animation: none !important;
        }

        *:hover {
            transform: none !important;
            box-shadow: none !important;
            filter: none !important;
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="nav-container">
            <a href="/home" class="logo-section">
                <img src="resources/2.png" alt="GIIS Clubs" class="logo">
            </a>
            <nav>
                <ul class="nav-links">
                    <li><a href="/home">Dashboard</a></li>
                    <li><a href="/linklist-manager">Linktree Manager</a></li>
                    <li><a href="/shorten">Shorten Links</a></li>
                    <li><a href="/qrcode">QR Generator</a></li>
                </ul>
            </nav>
            <div class="nav-actions">
                <a href="/home" class="cta-button">Back to Dashboard</a>
            </div>
        </div>
    </header>

    <main class="container">
        <div class="page-header">
            <div>
                <h1 class="page-title">Manage Club Profile</h1>
                <p class="page-subtitle">Update the information shown on Explore Clubs and your Linktree.</p>
            </div>
        </div>

        <% if (success) { %>
            <div class="message success"><%= success %></div>
        <% } %>
        <% if (error) { %>
            <div class="message error"><%= error %></div>
        <% } %>

        <div class="panel">
            <div class="panel-header">
                <h2>Club Profile Settings</h2>
                <p>Keep your club profile fresh across Explore and Linktree.</p>
            </div>
            <form action="/manage-club" method="POST" enctype="multipart/form-data">
                <input type="hidden" name="current_club" value="<%= club?.name || '' %>">

                <div class="section-block">
                    <div class="section-heading">
                        <h3>Basics</h3>
                        <span>Name and description used across the site.</span>
                    </div>
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="description">Club Description</label>
                            <textarea id="description" name="description" placeholder="Describe your club..." required><%= club?.description || '' %></textarea>
                        </div>
                    </div>
                </div>

                <div class="section-block">
                    <div class="section-heading">
                        <h3>Explore Link</h3>
                        <span>This is the button shown on your Explore card.</span>
                    </div>
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="link_text">Explore Link Text</label>
                            <input type="text" id="link_text" name="link_text" value="<%= club?.link_text || '' %>" placeholder="e.g., Visit Instagram">
                        </div>
                        <div class="form-group">
                            <label for="link">Explore Link URL</label>
                            <input type="url" id="link" name="link" value="<%= club?.link || '' %>" placeholder="https://example.com">
                        </div>
                    </div>
                </div>

                <div class="section-block">
                    <div class="section-heading">
                        <h3>Branding Assets</h3>
                        <span>Upload images to update your club branding.</span>
                    </div>
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="banner_upload">Club Banner</label>
                            <input type="file" id="banner_upload" name="banner_upload" accept="image/*">
                            <span class="helper-text">Banner for Explore cards. Uploaded as club.ext</span>
                        </div>
                        <div class="form-group">
                            <label for="logo_p_upload">Profile Logo</label>
                            <input type="file" id="logo_p_upload" name="logo_p_upload" accept="image/*">
                            <span class="helper-text">Profile avatar for Linktree. Uploaded as club-p.ext</span>
                        </div>
                    </div>
                </div>

                <div class="actions">
                    <button type="submit" class="btn-primary">Save Changes</button>
                </div>
            </form>
        </div>
    </main>
</body>
</html>
