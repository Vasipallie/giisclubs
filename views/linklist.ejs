<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= clubName %> - GIIS Clubs.</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            min-height: 100vh;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(25, 71, 184, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(226, 27, 35, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 50% 20%, rgba(237, 135, 3, 0.1) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        .container {
            max-width: 680px;
            margin: 0 auto;
            padding: 40px 0;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .header {
            margin-bottom: 48px;
            animation: fadeInDown 0.6s ease;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .club-icon {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #1947b8 0%, #e21b23 50%, #ed8703 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 48px;
            margin: 0 auto 24px;
            font-weight: 700;
            box-shadow: 
                0 8px 32px rgba(25, 71, 184, 0.3),
                0 0 60px rgba(226, 27, 35, 0.2),
                inset 0 0 20px rgba(255, 255, 255, 0.1);
            position: relative;
        }

        .club-icon::after {
            content: '';
            position: absolute;
            inset: -3px;
            border-radius: 50%;
            padding: 3px;
            background: linear-gradient(135deg, #1947b8, #e21b23, #ed8703);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0.6;
        }

        .club-name {
            font-size: 28px;
            font-weight: 700;
            color: #fff;
            margin-bottom: 12px;
            letter-spacing: -0.03em;
        }

        .club-subtitle {
            font-size: 16px;
            color: #a1a1aa;
            font-weight: 400;
        }

        .links-container {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-top: 32px;
            padding: 0 12px;
        }

        .link-button {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 22px 32px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 2px solid transparent;
            border-radius: 16px;
            text-decoration: none;
            color: #0a0a0a;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
            box-shadow: 0 4px 20px rgba(255, 255, 255, 0.1);
            position: relative;
            animation: fadeInUp 0.6s ease backwards;
        }

        .link-button:nth-child(1) { animation-delay: 0.1s; }
        .link-button:nth-child(2) { animation-delay: 0.2s; }
        .link-button:nth-child(3) { animation-delay: 0.3s; }
        .link-button:nth-child(4) { animation-delay: 0.4s; }
        .link-button:nth-child(5) { animation-delay: 0.5s; }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .link-button:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 
                0 12px 40px rgba(25, 71, 184, 0.4),
                0 0 0 2px #1947b8;
            border-color: #1947b8;
            background: rgba(255, 255, 255, 1);
        }

        .link-button:active {
            transform: translateY(-2px) scale(1.01);
        }

        .link-headline {
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            color: #0a0a0a;
            transition: color 0.3s ease;
        }

        .link-button:hover .link-headline {
            color: #1947b8;
        }

        .link-url {
            display: none;
        }

        .no-links {
            color: #71717a;
            padding: 60px 20px;
            font-size: 16px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 16px;
            border: 1px dashed rgba(255, 255, 255, 0.1);
        }

        .footer {
            margin-top: 80px;
            font-size: 14px;
            color: #52525b;
            animation: fadeIn 0.6s ease 0.6s backwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @media (max-width: 600px) {
            .container {
                padding: 30px 0;
            }

            .club-icon {
                width: 100px;
                height: 100px;
                font-size: 40px;
            }

            .club-name {
                font-size: 24px;
            }
            
            .links-container {
                padding: 0 8px;
                gap: 12px;
            }

            .link-button {
                padding: 20px 24px;
            }
        }
        /* Disable hover animations */
        *, *::before, *::after {
            transition: none !important;
            animation: none !important;
        }

        *:hover {
            transform: none !important;
            box-shadow: none !important;
            filter: none !important;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <% if (logoPath) { %>
                <div class="club-icon" style="background: transparent; box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);">
                    <img src="<%= logoPath %>" alt="<%= clubName %>" style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;">
                </div>
            <% } else { %>
                <div class="club-icon">
                    <%= clubName.charAt(0).toUpperCase() %>
                </div>
            <% } %>
            <div class="club-name"><%= clubName %></div>
            <div class="club-subtitle">All links in one place</div>
        </div>

        <div class="links-container">
            <% if (links && links.length > 0) { %>
                <% links.forEach(link => { %>
                    <a href="<%= link.url %>" target="_blank" rel="noopener noreferrer" class="link-button">
                        <div class="link-headline"><%= link.headline %></div>
                        <div class="link-url"><%= link.url.replace(/^https?:\/\/(www\.)?/, '').substring(0, 40) %><%= link.url.length > 40 ? '...' : '' %></div>
                    </a>
                <% }); %>
            <% } else { %>
                <div class="no-links">No links added yet</div>
            <% } %>
        </div>

        <div class="footer">
            Powered by GIIS Clubs.
        </div>
    </div>
</body>
</html>
