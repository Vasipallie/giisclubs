<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GIIS Clubs. | Link Manager</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --ink: #0f172a;
            --muted: #64748b;
            --surface: #ffffff;
            --surface-soft: #f8fafc;
            --border: #e2e8f0;
            --sidebar-width: 260px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #f5f7fb;
            color: #111827;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            bottom: 0;
            width: var(--sidebar-width);
            background: var(--surface);
            border-right: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            z-index: 1000;
        }

        .sidebar-header {
            padding: 24px;
            border-bottom: 1px solid var(--border);
        }

        .sidebar-logo {
            height: 48px;
            width: auto;
        }

        .sidebar-nav {
            flex: 1;
            padding: 16px 0;
            overflow-y: auto;
        }

        .nav-section {
            margin-bottom: 8px;
        }

        .nav-section-title {
            padding: 8px 24px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--muted);
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 24px;
            color: var(--ink);
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            border-left: 3px solid transparent;
        }

        .nav-link:hover {
            background: var(--surface-soft);
            color: var(--primary);
        }

        .nav-link.active {
            background: rgba(37, 99, 235, 0.08);
            color: var(--primary);
            border-left-color: var(--primary);
            font-weight: 600;
        }

        .nav-icon {
            width: 20px;
            height: 20px;
            stroke: currentColor;
            stroke-width: 2;
            fill: none;
        }

        .sidebar-footer {
            padding: 16px 24px;
            border-top: 1px solid var(--border);
        }

        .club-info {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .club-avatar {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 16px;
        }

        .club-avatar-img {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            object-fit: cover;
        }

        .club-details {
            flex: 1;
            min-width: 0;
        }

        .club-name {
            font-size: 14px;
            font-weight: 600;
            color: var(--ink);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .club-status {
            font-size: 12px;
            color: var(--muted);
        }

        .logout-btn {
            width: 100%;
            padding: 10px 0;
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: 6px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 8px;
            transition: background 0.2s;
        }
        .logout-btn:hover {
            background: var(--primary-dark);
        }

        /* Main Content */
        .main-content {
            margin-left: var(--sidebar-width);
            min-height: 100vh;
        }

        .content-area {
            padding: 32px;
            max-width: 1200px;
        }

        /* Mobile Toggle */
        .mobile-toggle {
            display: none;
            position: fixed;
            top: 16px;
            left: 16px;
            z-index: 1001;
            width: 44px;
            height: 44px;
            border-radius: 10px;
            background: var(--surface);
            border: 1px solid var(--border);
            cursor: pointer;
            align-items: center;
            justify-content: center;
        }

        .mobile-toggle svg {
            width: 24px;
            height: 24px;
            stroke: var(--ink);
            stroke-width: 2;
            fill: none;
        }

        .sidebar-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 999;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .sidebar.open {
                transform: translateX(0);
            }

            .sidebar-overlay.open {
                display: block;
            }

            .main-content {
                margin-left: 0;
            }

            .mobile-toggle {
                display: flex;
            }

            .content-area {
                padding: 24px 16px;
                padding-top: 72px;
            }
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 0;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
            padding: 22px 24px;
            background: #ffffff;
            border-radius: 16px;
            border: 1px solid #e5e7eb;
            box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
        }

        .header h1 {
            font-size: 28px;
            margin-bottom: 6px;
            color: #111827;
        }

        .header-subtitle {
            color: #6b7280;
            font-size: 14px;
        }

        .header-buttons {
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 10px 18px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-size: 13px;
            font-weight: 600;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: #1a237e;
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(26, 35, 126, 0.25);
        }

        .btn-secondary {
            background: #eef2f7;
            color: #1f2937;
            border: 1px solid #e5e7eb;
        }

        .btn-secondary:hover {
            background: #e5e7eb;
        }

        .btn-danger {
            background: #ef4444;
            color: white;
            padding: 8px 12px;
            font-size: 12px;
        }

        .btn-danger:hover {
            background: #dc2626;
        }

        .btn-edit {
            background: #2563eb;
            color: white;
            padding: 8px 12px;
            font-size: 12px;
        }

        .btn-edit:hover {
            background: #1d4ed8;
        }

        .form-section {
            background: #ffffff;
            padding: 24px;
            border-radius: 16px;
            border: 1px solid #e5e7eb;
            box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
            margin-bottom: 24px;
        }

        .form-section h2 {
            font-size: 18px;
            margin-bottom: 16px;
            color: #111827;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #111827;
            font-size: 13px;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px 14px;
            border: 1px solid #e5e7eb;
            border-radius: 12px;
            font-size: 14px;
            font-family: inherit;
            transition: border-color 0.2s, box-shadow 0.2s;
            background: #ffffff;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #1a237e;
            box-shadow: 0 0 0 3px rgba(26, 35, 126, 0.12);
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .links-list {
            background: #ffffff;
            padding: 24px;
            border-radius: 16px;
            border: 1px solid #e5e7eb;
            box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
        }

        .links-list h2 {
            font-size: 18px;
            margin-bottom: 16px;
            color: #111827;
        }

        .link-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px;
            background: #f8fafc;
            border-radius: 12px;
            margin-bottom: 12px;
            border: 1px solid #e5e7eb;
            border-left: 4px solid #1a237e;
            cursor: grab;
            gap: 12px;
        }

        .link-item:active {
            cursor: grabbing;
        }

        .drag-handle {
            color: #9ca3af;
            font-weight: bold;
            font-size: 18px;
            user-select: none;
            flex-shrink: 0;
        }

        .link-info {
            flex: 1;
            min-width: 0;
        }

        .link-headline {
            font-weight: 600;
            font-size: 15px;
            color: #111827;
            margin-bottom: 6px;
        }

        .link-url {
            font-size: 13px;
            color: #6b7280;
            word-break: break-all;
        }

        .link-actions {
            display: flex;
            gap: 8px;
            flex-shrink: 0;
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 2000;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background: #ffffff;
            border-radius: 16px;
            padding: 24px;
            max-width: 500px;
            width: 90%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .modal-header h3 {
            font-size: 20px;
            color: #111827;
        }

        .modal-close {
            background: none;
            border: none;
            font-size: 28px;
            color: #6b7280;
            cursor: pointer;
            padding: 0;
            width: 32px;
            height: 32px;
        }

        .modal-actions {
            display: flex;
            gap: 12px;
            justify-content: flex-end;
            margin-top: 20px;
        }

        .no-links-message {
            text-align: center;
            padding: 40px 20px;
            color: #999;
        }

        .message {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .message.success {
            background: #d1fae5;
            color: #065f46;
            border: 1px solid #a7f3d0;
        }

        .message.error {
            background: #fee2e2;
            color: #991b1b;
            border: 1px solid #fecaca;
        }

        .linklist-link {
            text-align: center;
            padding: 12px 18px;
            background: #1a237e;
            color: white;
            border-radius: 12px;
            text-decoration: none;
            display: inline-block;
            margin-bottom: 20px;
            font-weight: 600;
            transition: all 0.2s;
        }

        .linklist-link:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(26, 35, 126, 0.25);
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .form-row {
                grid-template-columns: 1fr;
            }

            .header {
                flex-direction: column;
                text-align: center;
                gap: 15px;
            }

            .link-item {
                flex-direction: column;
                align-items: flex-start;
            }

            .link-actions {
                width: 100%;
                margin-top: 10px;
            }
        }

        .loading {
            display: none;
            text-align: center;
            padding: 20px;
        }

        .loading.show {
            display: block;
        }
        /* Disable hover animations */
        *, *::before, *::after {
            transition: none !important;
            animation: none !important;
        }

        *:hover {
            transform: none !important;
            box-shadow: none !important;
            filter: none !important;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <% if (clubLogo) { %>
                <img src="<%= clubLogo %>" alt="Club Logo" class="sidebar-logo">
            <% } else { %>
                <img src="resources/2.png" alt="GIIS Clubs" class="sidebar-logo">
            <% } %>
        </div>

        <nav class="sidebar-nav">
            <!-- Main Section -->
            <div class="nav-section">
                <div class="nav-section-title">Main</div>
                <a href="/home" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                        <polyline points="9 22 9 12 15 12 15 22"></polyline>
                    </svg>
                    Dashboard
                </a>
            </div>

            <!-- Events Section -->
            <div class="nav-section">
                <div class="nav-section-title">Events</div>
                <a href="/add-event" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <line x1="12" y1="5" x2="12" y2="19"></line>
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                    </svg>
                    Create Event
                </a>
                <a href="/events" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <rect x="3" y="4" width="18" height="18" rx="2"></rect>
                        <line x1="9" y1="9" x2="15" y2="9"></line>
                        <line x1="9" y1="15" x2="15" y2="15"></line>
                    </svg>
                    All Club Events
                </a>
                <a href="/manage-events" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                        <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                    </svg>
                    Manage My Events
                </a>
            </div>

            <!-- Tools Section -->
            <div class="nav-section">
                <div class="nav-section-title">Tools</div>
                <a href="/shorten" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"></path>
                        <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"></path>
                    </svg>
                    Link Shortener
                </a>
                <a href="/qrcode" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <rect x="3" y="3" width="7" height="7"></rect>
                        <rect x="14" y="3" width="7" height="7"></rect>
                        <rect x="14" y="14" width="7" height="7"></rect>
                        <rect x="3" y="14" width="4" height="4"></rect>
                    </svg>
                    QR Generator
                </a>
                <a href="/linklist-manager" class="nav-link active">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"></path>
                        <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"></path>
                    </svg>
                    Linklist Manager
                </a>
            </div>

            <% if (isAdmin) { %>
            <!-- Admin Section -->
            <div class="nav-section">
                <div class="nav-section-title">Admin</div>
                <a href="/admin-dashboard" class="nav-link">
                    <svg class="nav-icon" viewBox="0 0 24 24">
                        <path d="M12 2L2 7l10 5 10-5-10-5z"></path>
                        <polyline points="2 17 12 22 22 17"></polyline>
                        <polyline points="2 12 12 17 22 12"></polyline>
                    </svg>
                    Admin Dashboard
                </a>
            </div>
            <% } %>
        </nav>

        <!-- Sidebar Footer -->
        <div class="sidebar-footer">
            <div class="club-info">
                <% if (clubLogo) { %>
                    <img src="<%= clubLogo %>" alt="<%= clubName %>" class="club-avatar-img">
                <% } else { %>
                    <div class="club-avatar"><%= (clubName || 'C').charAt(0).toUpperCase() %></div>
                <% } %>
                <div class="club-details">
                    <div class="club-name"><%= clubName || 'Club' %></div>
                    <div class="club-status">Member</div>
                </div>
            </div>
            <form action="/logout" method="post" style="width: 100%;">
                <button type="submit" class="logout-btn">Logout</button>
            </form>
        </div>
    </aside>

    <!-- Mobile Toggle -->
    <button class="mobile-toggle" id="mobile-toggle">
        <svg viewBox="0 0 24 24">
            <line x1="3" y1="6" x2="21" y2="6"></line>
            <line x1="3" y1="12" x2="21" y2="12"></line>
            <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
    </button>
    <div class="sidebar-overlay" id="sidebar-overlay"></div>

    <main class="main-content">
        <div class="content-area">
            <div class="container">
                <div class="header">
                    <div>
                        <h1>Link Manager</h1>
                        <div class="header-subtitle">Manage your club's linklist</div>
                    </div>
            <div class="header-buttons">
                <a href="/home" class="btn btn-secondary">← Back</a>
                <a href="/linklist/<%= clubName %>" class="btn btn-primary" target="_blank">View Linklist</a>
            </div>
        </div>

        <div id="messageContainer"></div>

        <div class="form-section">
            <h2>Add New Link</h2>
            <form id="addLinkForm">
                <div class="form-group">
                    <label for="headline">Link Headline *</label>
                    <input type="text" id="headline" name="headline" placeholder="e.g., Follow Us on Instagram" required>
                </div>
                <div class="form-group">
                    <label for="url">URL *</label>
                    <input type="url" id="url" name="url" placeholder="https://example.com" required>
                </div>
                <button type="submit" class="btn btn-primary">Add Link</button>
                <div class="loading" id="addLoading">Adding link...</div>
            </form>
        </div>

        <div class="links-list">
            <h2>Your Links (<span id="linkCount">0</span>)</h2>
            <div id="linksList" class="sortable-list"></div>
        </div>
    </div>

    <!-- Edit Link Modal -->
    <div id="editModal" class="modal" style="display: none;">
        <div class="modal-content">
            <div class="modal-header">
                <h3>Edit Link</h3>
                <button class="modal-close" onclick="closeEditModal()">&times;</button>
            </div>
            <form id="editForm">
                <div class="form-group">
                    <label for="editHeadline">Headline *</label>
                    <input type="text" id="editHeadline" required>
                </div>
                <div class="form-group">
                    <label for="editUrl">URL *</label>
                    <input type="url" id="editUrl" required>
                </div>
                <div class="modal-actions">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <button type="button" class="btn btn-secondary" onclick="closeEditModal()">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        const clubName = '<%= clubName %>';
        let currentEditingLinkId = null;
        let draggedElement = null;

        // Load links on page load
        document.addEventListener('DOMContentLoaded', loadLinks);

        // Add link form submission
        document.getElementById('addLinkForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const headline = document.getElementById('headline').value.trim();
            const url = document.getElementById('url').value.trim();

            if (!headline || !url) {
                showMessage('Please fill in all fields', 'error');
                return;
            }

            const loading = document.getElementById('addLoading');
            loading.classList.add('show');

            try {
                const response = await fetch('/linklist/add', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        headline,
                        url,
                        club: clubName
                    })
                });

                const data = await response.json();

                if (!response.ok) {
                    throw new Error(data.error || 'Failed to add link');
                }

                showMessage('Link added successfully!', 'success');
                document.getElementById('addLinkForm').reset();
                loadLinks();
            } catch (error) {
                console.error('Error:', error);
                showMessage(error.message, 'error');
            } finally {
                loading.classList.remove('show');
            }
        });

        async function loadLinks() {
            try {
                const response = await fetch(`/api/linklist/${clubName}`);
                const data = await response.json();

                if (!response.ok) {
                    throw new Error(data.error || 'Failed to load links');
                }

                const links = data.links || [];
                const linksList = document.getElementById('linksList');
                const linkCount = document.getElementById('linkCount');

                linkCount.textContent = links.length;

                if (links.length === 0) {
                    linksList.innerHTML = '<div class="no-links-message">No links added yet. Create your first link above!</div>';
                    return;
                }

                linksList.innerHTML = links.map(link => `
                    <div class="link-item" draggable="true" data-id="${link.id}">
                        <div class="drag-handle">::</div>
                        <div class="link-info">
                            <div class="link-headline">${escapeHtml(link.headline)}</div>
                            <div class="link-url">${escapeHtml(link.url)}</div>
                        </div>
                        <div class="link-actions">
                            <button class="btn btn-edit" onclick="openEditModal('${link.id}', '${escapeHtml(link.headline).replace(/'/g, "\\'")}', '${escapeHtml(link.url).replace(/'/g, "\\'")}')">Edit</button>
                            <button class="btn btn-danger" onclick="deleteLink('${link.id}')">Delete</button>
                        </div>
                    </div>
                `).join('');

                // Add drag-and-drop listeners
                setupDragAndDrop();
            } catch (error) {
                console.error('Error loading links:', error);
                document.getElementById('linksList').innerHTML = '<div class="no-links-message">Failed to load links</div>';
            }
        }

        function setupDragAndDrop() {
            const items = document.querySelectorAll('.link-item');
            
            items.forEach(item => {
                item.addEventListener('dragstart', handleDragStart);
                item.addEventListener('dragend', handleDragEnd);
                item.addEventListener('dragover', handleDragOver);
                item.addEventListener('drop', handleDrop);
            });
        }

        function handleDragStart(e) {
            draggedElement = this;
            this.style.opacity = '0.5';
            e.dataTransfer.effectAllowed = 'move';
        }

        function handleDragEnd(e) {
            this.style.opacity = '1';
            draggedElement = null;
        }

        function handleDragOver(e) {
            if (e.preventDefault) {
                e.preventDefault();
            }
            e.dataTransfer.dropEffect = 'move';
            return false;
        }

        function handleDrop(e) {
            if (e.stopPropagation) {
                e.stopPropagation();
            }

            if (draggedElement !== this) {
                const list = document.getElementById('linksList');
                const items = Array.from(list.children);
                const draggedIndex = items.indexOf(draggedElement);
                const targetIndex = items.indexOf(this);

                if (draggedIndex < targetIndex) {
                    this.parentNode.insertBefore(draggedElement, this.nextSibling);
                } else {
                    this.parentNode.insertBefore(draggedElement, this);
                }

                saveOrder();
            }

            return false;
        }

        async function saveOrder() {
            const items = document.querySelectorAll('.link-item');
            const links = Array.from(items).map((item, index) => ({
                id: item.dataset.id,
                order: index
            }));

            try {
                const response = await fetch('/linklist/reorder', {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ links })
                });

                if (!response.ok) {
                    const data = await response.json();
                    throw new Error(data.error || 'Failed to save order');
                }

                showMessage('Order saved!', 'success');
                loadLinks();
            } catch (error) {
                console.error('Error:', error);
                showMessage('Failed to save order: ' + error.message, 'error');
                loadLinks();
            }
        }

        function openEditModal(linkId, headline, url) {
            currentEditingLinkId = linkId;
            document.getElementById('editHeadline').value = headline;
            document.getElementById('editUrl').value = url;
            document.getElementById('editModal').style.display = 'flex';
        }

        function closeEditModal() {
            document.getElementById('editModal').style.display = 'none';
            currentEditingLinkId = null;
        }

        document.getElementById('editForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const headline = document.getElementById('editHeadline').value.trim();
            const url = document.getElementById('editUrl').value.trim();

            if (!headline || !url) {
                showMessage('Please fill in all fields', 'error');
                return;
            }

            try {
                const response = await fetch(`/linklist/${currentEditingLinkId}/edit`, {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ headline, url })
                });

                const data = await response.json();

                if (!response.ok) {
                    throw new Error(data.error || 'Failed to update link');
                }

                showMessage('Link updated successfully!', 'success');
                closeEditModal();
                loadLinks();
            } catch (error) {
                console.error('Error:', error);
                showMessage(error.message, 'error');
            }
        });

        async function deleteLink(linkId) {
            if (!confirm('Are you sure you want to delete this link?')) return;

            try {
                const response = await fetch(`/linklist/${linkId}`, {
                    method: 'DELETE'
                });

                const data = await response.json();

                if (!response.ok) {
                    throw new Error(data.error || 'Failed to delete link');
                }

                showMessage('Link deleted successfully!', 'success');
                loadLinks();
            } catch (error) {
                console.error('Error:', error);
                showMessage(error.message, 'error');
            }
        }

        function showMessage(message, type) {
            const container = document.getElementById('messageContainer');
            const messageEl = document.createElement('div');
            messageEl.className = `message ${type}`;
            messageEl.textContent = message;
            container.innerHTML = '';
            container.appendChild(messageEl);

            setTimeout(() => {
                messageEl.remove();
            }, 5000);
        }

        function escapeHtml(text) {
            const map = {
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                '"': '&quot;',
                "'": '&#039;'
            };
            return text.replace(/[&<>"']/g, m => map[m]);
        }

        // Mobile sidebar toggle
        const mobileToggle = document.getElementById('mobile-toggle');
        const sidebar = document.getElementById('sidebar');
        const sidebarOverlay = document.getElementById('sidebar-overlay');

        mobileToggle.addEventListener('click', () => {
            sidebar.classList.toggle('open');
            sidebarOverlay.classList.toggle('open');
        });

        sidebarOverlay.addEventListener('click', () => {
            sidebar.classList.remove('open');
            sidebarOverlay.classList.remove('open');
        });

        // Close sidebar when a link is clicked
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', () => {
                if (window.innerWidth <= 768) {
                    sidebar.classList.remove('open');
                    sidebarOverlay.classList.remove('open');
                }
            });
        });
    </script>
            </div>
        </div>
    </main>
</body>
</html>
